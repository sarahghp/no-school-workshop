// reset
solid().out()

// make an oscillator
osc().out()

// change frequency
osc(100).out()

// change sync
osc(100, .01).out()

// using offset to change colors
osc(60, .1, 100).out()

osc(60, .1, 500).out()

osc(60, .1, 900).out()

osc(60, .1, 0).out()
